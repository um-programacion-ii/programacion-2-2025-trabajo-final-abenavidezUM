# Base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=eventos_db
DB_USER=postgres
DB_PASSWORD=postgres

# Redis Local (para sesiones del backend)
REDIS_LOCAL_HOST=localhost
REDIS_LOCAL_PORT=6379

# Redis Cátedra (actualizar cuando se proporcione acceso)
REDIS_CATEDRA_HOST=localhost
REDIS_CATEDRA_PORT=6380
REDIS_CATEDRA_PASSWORD=

# Kafka
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_CONSUMER_GROUP_ID=alumno_tu_id

# Servicio de Cátedra (actualizar cuando se proporcione)
CATEDRA_API_URL=http://localhost:9090
CATEDRA_API_TOKEN=

# JWT
JWT_SECRET=tu_secreto_super_seguro_cambiar_en_produccion_minimo_256_bits
JWT_EXPIRATION_SECONDS=3600

# Backend
BACKEND_PORT=8080
BACKEND_CONTEXT_PATH=/api

# Proxy
PROXY_PORT=8081

# Sesiones
SESSION_TIMEOUT_MINUTES=30
SEAT_BLOCK_TIMEOUT_MINUTES=5
